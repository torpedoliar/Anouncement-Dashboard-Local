---
phase: 11-portal-auth-surfaces
plan: 04
type: execute
wave: 3
depends_on:
  - 11-02
  - 11-03
files_modified:
  - app/admin/portal-sessions/page.tsx
  - app/admin/portal-audit/page.tsx
  - app/admin/portal-users/page.tsx
  - app/admin/portal-apps/page.tsx
  - app/admin/portal-groups/page.tsx
  - .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-E2E-CHECKLIST.md
autonomous: true
requirements:
  - UIUX-05
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "All five admin ledger pages render token-native on the Table-kit family (Card + TableColumn sortKey + aria-sort + Badge + kit Button/Select/Input + ConfirmDialog), filters and export byte-identical: portal-sessions keeps portalUserId select + page/limit=20 pagination with same fetch URL construction; portal-audit keeps its 5 tabs (Ringkasan / Deteksi Account Sharing / Unused Access / Access Control Matrix / Histori Pencabutan) + inline CSV export from the table's id list (exportToCSV logic and header order untouched, DownloadSimple ghost buttons); portal-users keeps create/edit/delete/reset-password/access modals with same POST/PUT/DELETE bodies; portal-apps/portal-groups keep isPublic toggle and Deteksi Otomatis flow untouched."
    - "Status and role semantics rendered via the Badge kit: session status Aktif (success) / Kedaluwarsa (warning) / Dicabut (neutral); user status aktif (success) / nonaktif (neutral); role PORTAL_ADMIN (danger + ShieldCheck) / PORTAL_USER (neutral + CircleUser); app isPublic 'Publik' (success) / 'Terbatas' (neutral + LockKey)."
    - "Ledger row meta uses the mono/numeric discipline: user counts, session counts, and 'Terakhir dipakai' values in font-mono tabular-nums text-xs text-text-3; empty states use the exact copy ('Belum ada pengguna.', 'Belum ada sesi.', 'Belum ada data audit.', 'Belum ada aplikasi.', 'Belum ada grup.') with icon-tile card presentation."
    - "Regression evidence is recorded in .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-E2E-CHECKLIST.md covering the 8-step manual flow (portal login → grid → save credential → SSO auto-submit → account picker → failure path → lockout → audit rows), plus OPD-1 git-diff-empty on the five frozen files (lib/portal-access.ts, lib/portal-layout.ts, lib/portal-auth.ts, lib/auth.ts, middleware.ts), plus OPD-4 scan proving no stale portalUserId_appId compound lookups in the phase diff, plus git diff --name-only scope check proving the phase touched UI files only."
  artifacts:
    - app/admin/portal-sessions/page.tsx, portal-audit/page.tsx, portal-users/page.tsx, portal-apps/page.tsx, portal-groups/page.tsx (restyled, behavior-identical)
    - .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-E2E-CHECKLIST.md (regression evidence)
  key_links:
    - Table kit sortKey/aria-sort wiring drives the same server sort params (orderBy/sort) — unchanged URL construction
    - portal-audit CSV export derives row ids from the same loaded table rows — unchanged
    - users modals POST/PUT/DELETE against /api/admin/portal-users and /api/admin/portal-sessions revoke — unchanged
  prohibitions:
    - statement: MUST NOT alter filter selects, pagination construction (page, limit=20, portalUserId), fetch URLs, sort params, or export header order in the five ledgers
      status: resolved
      verification: diff on query/URL lines unchanged; grep for page= & limit= & exportToCSV intact
    - statement: MUST NOT change API bodies or status/role labels rendered from server data (status derived, not remapped client-side)
      status: resolved
      verification: judgment — diff on label maps unchanged; server text stays verbatim
    - statement: MUST NOT touch lib/portal-access.ts, lib/portal-layout.ts, lib/portal-auth.ts, lib/auth.ts, middleware.ts (OPD-1)
      status: resolved
      verification: git diff empty on those files after the plan
    - statement: MUST NOT introduce stale portalUserId_appId compound lookups (OPD-4)
      status: resolved
      verification: grep on phase diff shows no compound compound-key access
    - statement: MUST NOT introduce legacy icon imports, inline style objects, raw hex, or new capabilities (no timeline rail on portal-audit per D-07)
      status: resolved
      verification: static scoped grep per D-08
  backstops:
    - statement: Ledger pagination/filter interactions hold in a browser session (manual E2E)
      verification: backstop — E2E checklist step 6 covers revoke + audit visibility when env available
  assumptions:
    - "portal-apps/portal-groups pages exist with isPublic toggle + Deteksi Otomatis (per admin inventory); the phase restyles only, keeping bodies."
    - "CSV export stays inline on portal-audit (existing exportToCSV); no new file-download abstraction is introduced."
---

<objective>
Portal admin ledgers token-native + regression evidence for the whole phase.

Purpose: The final plan converts the five admin-facing ledger pages (sessions, audit, users, apps, groups) to the token/Table-kit pattern with byte-identical filters, export, and pagination — then records the phase's regression evidence (E2E checklist + OPD rechecks) so SPEC R5's "no behavior change" acceptance is provable after the phase.

Output: 5 restyled admin pages + `11-E2E-CHECKLIST.md` evidence file. Depends on 11-02/11-03 so Task 4's diff audit runs after all UI commits exist.

Frozen behavior: filter selects, page/limit=20 pagination, fetch URLs, sort params, CSV export header order, modal API bodies, status/role text from server, Deteksi Otomatis flow. OPD-1/OPD-4 respected.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-CONTEXT.md
@.planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-SPEC.md
@.planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-UI-SPEC.md
@app/admin/portal-sessions/page.tsx
@app/admin/portal-audit/page.tsx
@app/admin/portal-users/page.tsx
@app/admin/portal-apps/page.tsx
@app/admin/portal-groups/page.tsx
@app/admin/users/page.tsx
@components/ui/Table.tsx
@components/ui/Badge.tsx
@components/ui/Button.tsx
@components/ui/Input.tsx
@components/ui/Select.tsx
@components/ui/ConfirmDialog.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ledger sesi & audit — Table-kit family, filter/export identik</name>
  <files>app/admin/portal-sessions/page.tsx, app/admin/portal-audit/page.tsx</files>
  <read_first>
    - app/admin/portal-sessions/page.tsx, app/admin/portal-audit/page.tsx (file yang diubah — kontrak fetch/filter/export)
    - app/admin/users/page.tsx (pola ledger Phase 10 yang direplikasi)
    - components/ui/Table.tsx, Badge.tsx, Button.tsx, Select.tsx, ConfirmDialog.tsx (kit)
    - .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-UI-SPEC.md §5 (kontrak ledger admin)
    - app/api/admin/portal-sessions/route.ts, app/api/admin/portal-audit/route.ts (kontrak API — JANGAN diubah)
  </read_first>
  <acceptance_criteria>
    - portal-sessions: stats tiles token-native (TOTAL/AKTIF/DICABUT via kit Card + font-display text-2xl + text-sm text-text-3 label); filter Select (semua user / portalUserId) dengan fetch URL identik (`/api/admin/portal-sessions?page=${page}&limit=20` + conditional portalUserId); pagination page/limit=20 + Prev/Next disabled state dipertahankan; status Badge Aktif (success) / Kedaluwarsa (warning) / Dicabut (neutral); aksi "Cabut" danger ghost → kit ConfirmDialog "Cabut Sesi?" (danger variant) → DELETE /api/admin/portal-sessions/{id}; useConfirm hook dipertahankan; teks status server verbatim.
    - portal-audit: 5 tab dipertahankan (Ringkasan / Deteksi Account Sharing / Unused Access / Access Control Matrix / Histori Pencabutan) dengan tab bar token (border-b border-border + tab aktif bg-accent-subtle text-accent); tabel per tab memakai Table kit; export CSV memakai DownloadSimple ghost (bukan teks "Download CSV") memicu exportToCSV yang SAMA (header order + pemisah koma + escaping identik); pemetaan ikon: FiDownload→DownloadSimple, FiRefreshCw→ArrowClockwise, FiShield→ShieldCheck, FiClock→Clock, FiUserX→UserMinus; tanpa timeline rail (D-07 — portal-audit tetap tabel).
    - Copy dan badge: "AKTIF"/"DICABUT"/"KEDALUWARSA" dari server dirender verbatim via Badge tone mapping (bukan remap teks).
    - Static gates kedua file: `! grep -nE '#[0-9a-fA-F]{3,8}' <files>`; `! grep -n 'style=\{\{' <files>`; `! grep -n 'react-icons' <files>`; hanya size 12/14/20/24 + weight 400/600.
  </acceptance_criteria>
  <action>
    Tulis ulang chrome kedua halaman memakai kit Table/Badge/Button/Select/ConfirmDialog + Phosphor sesuai kontrak. Pertahankan: state (stats, rows, page, selectedPortalUserId, loading, confirm), useEffect fetch dengan URL construction identik (template literal + query param conditional), logika exportToCSV byte-identical (pindahkan ke fungsi utuh tanpa mengubah header array/join), tab state + konten per tab (ringkasan card, tabel deteksi/akses/matriks), dan useConfirm flow. Ganti ikon line-style per peta pemetaan acceptance criteria; hapus semua style object; jangan tambah fitur (tanpa filter tanggal baru, tanpa export per-tab). Untuk "Cabut": pertahankan konfirmasi dan DELETE call identik. Pesan commit dalam Bahasa Indonesia.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit (exit 0)
      npx eslint app/admin/portal-sessions/page.tsx app/admin/portal-audit/page.tsx (0 errors)
      ! grep -nE '#[0-9a-fA-F]{3,8}' app/admin/portal-sessions/page.tsx app/admin/portal-audit/page.tsx
      ! grep -n 'style=\{\{' app/admin/portal-sessions/page.tsx app/admin/portal-audit/page.tsx
      ! grep -n 'react-icons' app/admin/portal-sessions/page.tsx app/admin/portal-audit/page.tsx
      grep -c 'limit=20' app/admin/portal-sessions/page.tsx (>= 1 — pagination identik)
      grep -c 'exportToCSV' app/admin/portal-audit/page.tsx (>= 2 — definisi + pemanggilan utuh)
      grep -c 'portalUserId' app/admin/portal-sessions/page.tsx (>= 2 — filter dipertahankan)
    </automated>
  </verify>
  <done>Kedua ledger token-native dengan filter/pagination/export identik; badge status verbatim; gate hijau; komit Bahasa Indonesia.</done>
  <reversibility rating="reversible">Presentasi-only; exportToCSV adalah titik kritis — diff fungsi kosong wajib sebelum komit.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Ledger portal-users — role badge, modals kit-native, API identik</name>
  <files>app/admin/portal-users/page.tsx</files>
  <read_first>
    - app/admin/portal-users/page.tsx (file yang diubah — 866L, kontrak modal/fetch)
    - app/admin/users/page.tsx (pola ledger Phase 10)
    - components/ui/Table.tsx, Badge.tsx, Button.tsx, Input.tsx, Select.tsx, ConfirmDialog.tsx, Modal.tsx (kit)
    - .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-UI-SPEC.md §5 (kontrak user ledger)
  </read_first>
  <acceptance_criteria>
    - Table kit: kolom (User, Role, Status, Dibuat, Aksi) dengan TableColumn sortKey + aria-sort; fetch `/api/admin/portal-users?page=&limit=20` + query filter (search/role/status bila ada) identik; skeleton rows saat loading.
    - Role badge: PORTAL_ADMIN → tone danger + ShieldCheck 12; PORTAL_USER → tone neutral + CircleUser 12; teks role verbatim dari server. Status: aktif → success badge, nonaktif → neutral badge, teks verbatim.
    - Modals (create/edit/delete/reset-password/access) memakai kit Input/Select/Button + Modal; POST/PUT/DELETE bodies identik (`{name, nik?, portalRole, password?, status?}` dsb per kontrak API); ConfirmDialog existing copy ("Hapus Pengguna?", "Yakin ingin menghapus...") dipertahankan verbatim; tombol danger ghost dengan Trash 16.
    - Reset password: kit Button danger ghost + "Reset Password" copy dipertahankan; POST /api/admin/portal-users/{id}/reset-password identik. Access: kontrak hak akses (modal portal-apps permission) identik.
    - Empty state: "Belum ada pengguna." + icon-tile card; mono digits untuk statistik user (`font-mono tabular-nums`).
    - Static gates: `! grep -nE '#[0-9a-fA-F]{3,8}' <file>`; `! grep -n 'style=\{\{' <file>`; `! grep -n 'react-icons' <file>`; hanya size 12/14/20/24 + weight 400/600.
  </acceptance_criteria>
  <action>
    Tulis ulang chrome halaman portal-users memakai kit Table/Badge/Button/Input/Select/Modal/ConfirmDialog sesuai kontrak. Pertahankan state, fetch dengan URL construction identik, seluruh handler (handleCreate/handleUpdate/handleDelete/handleReset/handleAccess), dan body API byte-identical — pindahkan label/modal bodies tanpa mengubah nilai. Ganti ikon line-style (PencilSimple, Trash, Key, ShieldCheck, CircleUser, UserPlus) per peta pemetaan; hapus style object; jangan menambah fitur (tanpa bulk action baru, tanpa export). Status/role label dari server dirender verbatim — pemetaan warna hanya lewat Badge tone. Pesan commit dalam Bahasa Indonesia.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit (exit 0)
      npx eslint app/admin/portal-users/page.tsx (0 errors)
      ! grep -nE '#[0-9a-fA-F]{3,8}' app/admin/portal-users/page.tsx
      ! grep -n 'style=\{\{' app/admin/portal-users/page.tsx
      ! grep -n 'react-icons' app/admin/portal-users/page.tsx
      grep -c '/api/admin/portal-users' app/admin/portal-users/page.tsx (>= 4 — CRUD + reset dipertahankan)
      grep -c 'limit=20' app/admin/portal-users/page.tsx (>= 1 — pagination identik)
    </automated>
  </verify>
  <done>portal-users token-native dengan role badge sesuai skema RBAC, modal kit-native, API identik; gate hijau; komit Bahasa Indonesia.</done>
  <reversibility rating="reversible">Presentasi-only; body API adalah titik kritis — diff baris fetch wajib kosong.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Ledger portal-apps & portal-groups — isPublic, counts, Deteksi Otomatis utuh</name>
  <files>app/admin/portal-apps/page.tsx, app/admin/portal-groups/page.tsx</files>
  <read_first>
    - app/admin/portal-apps/page.tsx, app/admin/portal-groups/page.tsx (file yang diubah)
    - components/ui/Table.tsx, Badge.tsx, Button.tsx, Input.tsx, ConfirmDialog.tsx (kit)
    - .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-UI-SPEC.md §5 (kontrak app/group ledger)
    - app/api/admin/portal-apps/route.ts, app/api/admin/portal-groups/route.ts (kontrak API — JANGAN diubah)
  </read_first>
  <acceptance_criteria>
    - portal-apps: Table kit kolom (Nama, Slug, Grup, Visibilitas, Kredensial, Status, Aksi) — kolom dari tabel saat ini dipertahankan; badge isPublic "Publik" (success) / "Terbatas" (neutral + LockKey 12); count kredensial `font-mono tabular-nums text-xs`; create/edit modal dengan kit Input (nama/slug/grup/isPublic select) + body POST/PUT identik; "Deteksi Otomatis" flow (button ghost + ArrowClockwise 16 → POST /api/admin/portal-apps/detect) identik — tombol "Deteksi Otomatis" copy dipertahankan; hapus via ConfirmDialog "Hapus Aplikasi?".
    - portal-groups: Table kit kolom (Nama, Jumlah Aplikasi, Dibuat, Aksi); count mono; create/edit modal kit-native; DELETE /api/admin/portal-groups/{id} identik; empty "Belum ada grup."; tanpa fitur baru (tanpa drag-reorder — tidak ada di kontrak).
    - Static gates kedua file: `! grep -nE '#[0-9a-fA-F]{3,8}' <files>`; `! grep -n 'style=\{\{' <files>`; `! grep -n 'react-icons' <files>`; hanya size 12/14/20/24 + weight 400/600.
  </acceptance_criteria>
  <action>
    Tulis ulang chrome kedua halaman memakai kit sesuai kontrak. Pertahankan state, fetch URL construction, handler create/edit/delete/detect, dan body API byte-identical. Ganti ikon line-style (Plus, PencilSimple, Trash, ArrowClockwise, LockKey, SquaresFour) per peta pemetaan; hapus style object; status/copy verbatim dari server; jangan tambah fitur. Pesan commit dalam Bahasa Indonesia.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit (exit 0)
      npx eslint app/admin/portal-apps/page.tsx app/admin/portal-groups/page.tsx (0 errors)
      ! grep -nE '#[0-9a-fA-F]{3,8}' app/admin/portal-apps/page.tsx app/admin/portal-groups/page.tsx
      ! grep -n 'style=\{\{' app/admin/portal-apps/page.tsx app/admin/portal-groups/page.tsx
      ! grep -n 'react-icons' app/admin/portal-apps/page.tsx app/admin/portal-groups/page.tsx
      grep -c 'Deteksi Otomatis' app/admin/portal-apps/page.tsx (>= 1 — flow dipertahankan)
      grep -c '/api/admin/portal-groups' app/admin/portal-groups/page.tsx (>= 2 — CRUD dipertahankan)
    </automated>
  </verify>
  <done>Kedua ledger token-native dengan badge visibilitas, count mono, flow Deteksi Otomatis dan API identik; gate hijau; komit Bahasa Indonesia.</done>
  <reversibility rating="reversible">Presentasi-only; flow Deteksi Otomatis adalah titik kritis.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Bukti regresi — E2E checklist + recheck OPD-1/OPD-4 + scope diff</name>
  <files>.planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-E2E-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-SPEC.md §R5 (checklist E2E wajib)
    - .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-CONTEXT.md (OPD-1/OPD-4 wording)
    - .planning/STATE.md (PRE-1 — batasan env)
  </read_first>
  <acceptance_criteria>
    - 11-E2E-CHECKLIST.md dibuat dengan 8 langkah manual: (1) login portal (/portal-login) → grid "Aplikasi Saya" + chip filter; (2) simpan kredensial pertama (/portal/credentials?app=) → muncul di vault; (3) buka app → SSO auto-submit interstitial → landing di app target; (4) akun kedua → AccountSelector "Pilih Akun"; (5) jalur gagal: kredensial rusak → CorruptCredential → simpan ulang; (6) cabut sesi dari /admin/portal-sessions → akses portal ditolak; (7) lockout: NIK salah berulang → pesan "Akun terkunci..."; (8) audit: /admin/portal-audit menampilkan SSO_LAUNCH + revoke rows. Setiap langkah berisi Prasyarat / Langkah / Hasil yang diharapkan / Kolom hasil (dicentang manual bila env tersedia).
    - Recheck OPD-1: `git diff -- lib/portal-access.ts lib/portal-layout.ts lib/portal-auth.ts lib/auth.ts middleware.ts` KOSONG (frozen — hasil dicatat ke checklist).
    - Recheck OPD-4: grep pada diff phase (`git diff <base>..HEAD -- '*.ts' '*.tsx'`) menunjukkan TIDAK ADA akses compound key `portalUserId_appId` yang baru (dictum: tidak ada lookup kombinasi portalUserId+appId yang tersisa/stale).
    - Scope diff: `git diff --name-only <base>..HEAD` berisi HANYA file UI (components/**, app/**/*page.tsx, app/**/*layout.tsx portal) + docs — tanpa perubahan lib/**, api routes (kecuali pre-existing), prisma, atau middleware.
  </acceptance_criteria>
  <precondition>Seluruh komit UI plan 11-01..11-04 sudah ada di branch (git log menunjukkan plan 11-01..11-03 committed) — bila belum, tunda Task 4 hingga semua komit UI fase ini ada.</precondition>
  <action>
    Buat .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-E2E-CHECKLIST.md: header (judul, fase, tanggal, env-status PRE-1), tabel status gate (tsc/eslint/grep) yang sudah dijalankan per plan, 8 langkah manual dengan format Prasyarat/Langkah/Hasil yang diharapkan, dan bagian Recheck OPD-1 + OPD-4 + Scope Diff berisi hasil perintah git yang dijalankan saat task ini (jalankan perintahnya, tempel hasilnya ke file). Catat keterbatasan PRE-1 (build/dev rusak pre-existing — E2E browser menunggu env). Jangan mengubah file source apa pun dalam task ini — evidence-only. Pesan commit dalam Bahasa Indonesia.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-E2E-CHECKLIST.md
      grep -c 'OPD-1\|OPD-4' .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-E2E-CHECKLIST.md (>= 2)
      git diff --quiet -- lib/portal-access.ts lib/portal-layout.ts lib/portal-auth.ts lib/auth.ts middleware.ts && echo "OPD-1 CLEAN" (wajib keluar 0 — frozen)
    </automated>
  </verify>
  <done>Checklist E2E 8 langkah + recheck OPD-1/OPD-4 + scope diff tercatat; tidak ada file source diubah; komit Bahasa Indonesia.</done>
  <reversibility rating="reversible">Evidence-only; penghapusan aman.</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces (plan 11-04)

| Symbol / File | Kind | Used By |
|---------------|------|---------|
| `app/admin/portal-sessions/page.tsx` | RESTYLED (Table-kit family) | /admin/portal-sessions |
| `app/admin/portal-audit/page.tsx` | RESTYLED (5 tabs + export intact) | /admin/portal-audit |
| `app/admin/portal-users/page.tsx` | RESTYLED (RBAC badges, modals) | /admin/portal-users |
| `app/admin/portal-apps/page.tsx` | RESTYLED (isPublic, detect intact) | /admin/portal-apps |
| `app/admin/portal-groups/page.tsx` | RESTYLED (counts mono) | /admin/portal-groups |
| `11-E2E-CHECKLIST.md` | NEW evidence doc | verification phase |

No API route, lib, prisma, or middleware changes. Frozen files remain untouched (OPD-1 gate).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin ledgers → /api/admin/* | Server-backed ledger data crosses here; query construction (page/limit/portalUserId) frozen |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-15 | Tampering | ledger fetch/query construction | high | mitigate | Task gates assert page/limit=20, portalUserId filter, and sort params byte-identical; URL template strings untouched |
| T-11-16 | Tampering | CSV export logic | medium | mitigate | exportToCSV function moved intact (header order, escaping, delimiter unchanged); grep gate on function presence |
| T-11-17 | Spoofing | status/role label remap | low | mitigate | Status text rendered verbatim from server; Badge tone mapping only (no client-side text rewrite) |
| T-11-18 | Information disclosure | RBAC role badges | low | accept | Role values come from server session claims already displayed pre-rework; presentation-only change |
| T-11-19 | Tampering | reset-password / revoke actions | high | mitigate | POST/DELETE URLs and bodies unchanged; diff gates on fetch lines |
| T-11-SC | Tampering | npm installs | low | accept | No package installs in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` exit 0; scoped eslint 0 errors on the five pages.
- Static grep on touched files: 0 raw hex, 0 inline style objects, 0 legacy icon imports.
- Behavior-preservation greps: limit=20 + portalUserId + exportToCSV + Deteksi Otomatis + /api/admin/portal-users present.
- OPD-1: git diff empty on five frozen files; OPD-4: no new stale compound-key lookups; scope diff = UI-only.
- 11-E2E-CHECKLIST.md exists with 8 steps + gate table + OPD recheck results.
</verification>

<success_criteria>
- All five admin ledgers render token-native with byte-identical filters/export/pagination/API bodies (SPEC R2/R3 acceptance).
- RBAC + status semantics expressed via Badge kit; no client-side label rewriting (SPEC R4 pattern).
- Regression evidence file records E2E checklist + OPD-1/OPD-4/scope-diff results (SPEC R5 acceptance).
- `npx tsc --noEmit` exit 0 + scoped eslint 0 errors.
</success_criteria>

<output>
Create `.planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-04-SUMMARY.md` when done
</output>
