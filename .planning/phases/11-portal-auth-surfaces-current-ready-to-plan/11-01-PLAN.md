---
phase: 11-portal-auth-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/auth/AuthFrame.tsx
  - app/portal-login/page.tsx
  - app/(auth)/admin-login/page.tsx
autonomous: true
requirements:
  - UIUX-05
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Both login pages render the shared centered-card auth frame (48px bg-santos-red brand tile with white 'S' monogram, 'SANTOS JAYA ABADI' wordmark, eyebrow, title, subtitle) on tokens, paper/night parity via the same token set (D-04)."
    - "Portal login submit path is byte-identical: signIn(\"portal-credentials\", { redirect: false, callbackUrl: \"/portal\" }) — same cookie prefix portal-auth.*, same audit events, same redirects (D-05)."
    - "Invalid credentials render an inline danger region ON the card (border-danger/30 bg-danger-subtle + WarningCircle), never a blank-page toast; server Indonesian messages ('Password salah', 'NIK tidak ditemukan', 'Akun dinonaktifkan. Hubungi administrator.', 'Akun terkunci. Coba lagi dalam N menit.') kept verbatim; CredentialsSignin-type errors map to 'NIK atau password salah'; catch maps to 'Terjadi kesalahan. Silakan coba lagi.'"
    - "Admin login drops the split-panel branding column (D-04); 'Kembali ke Beranda' ghost link + ArrowLeft kept above the card; error 'Kredensial tidak valid' inline; CTA 'Masuk'/'Masuk...'; footer '© 2024 PT. Santos Jaya Abadi' centered under the card; signIn(\"credentials\", { redirect: false }) handler unchanged."
    - "Grep of the two touched pages + new component: zero legacy line-icon imports, zero inline style objects, zero raw hex literals — tokens + Phosphor only."
  artifacts:
    - components/auth/AuthFrame.tsx (new shared presentational component)
    - app/portal-login/page.tsx (restyled on AuthFrame)
    - app/(auth)/admin-login/page.tsx (restyled on AuthFrame, split panel removed)
  key_links:
    - app/portal-login/layout.tsx and app/(auth)/layout.tsx NextAuthProvider basePath props untouched (behavior link)
    - AuthFrame error slot wired to the existing per-page error state (result.error mapping)
  prohibitions:
    - statement: MUST NOT modify lib/portal-auth.ts / lib/auth.ts / middleware.ts (frozen)
      status: resolved
      verification: judgment — `git diff -- lib/portal-auth.ts lib/auth.ts middleware.ts` empty after the plan
    - statement: MUST NOT alter cookie names, NextAuth provider ids ('portal-credentials' / 'credentials'), redirect targets, or audit taxonomy
      status: resolved
      verification: judgment — diff on the two login pages shows UI-only changes; signIn call lines identical
    - statement: MUST NOT introduce legacy line-icon imports, inline style objects, or raw hex chrome in touched files
      status: resolved
      verification: scoped static grep (see task acceptance criteria)
---

<objective>
Shared auth frames for portal and admin login — token-native, behavior-identical.

Purpose: This is the phase's tracer slice. It proves the full token/Phosphor/kit pattern end-to-end on the two most behavior-sensitive surfaces (the NextAuth login paths), so every later plan in the phase repeats a proven recipe instead of inventing one. It also delivers the UI-SPEC §1 auth contract (centered card, brand mark, inline error) for both frames, satisfying SPEC R1 and CONTEXT D-04/D-05.

Output: new `components/auth/AuthFrame.tsx` shared frame component; `app/portal-login/page.tsx` and `app/(auth)/admin-login/page.tsx` restyled on it; per-frame inline error mapping with the locked copy contract.

Frozen behavior (restyle only — MUST NOT change): NextAuth provider ids, cookie prefixes, callbackUrl/redirect targets, lockout/audit logic, `lib/portal-auth.ts` / `lib/auth.ts` / `middleware.ts`. Gates: `npx tsc --noEmit` + scoped eslint + static grep per D-08; `npm run build`/dev render remain broken pre-existing (PRE-1) — screenshots are bonus only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-CONTEXT.md
@.planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-SPEC.md
@.planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-UI-SPEC.md
@app/portal-login/page.tsx
@app/portal-login/layout.tsx
@app/(auth)/admin-login/page.tsx
@app/(auth)/layout.tsx
@components/ui/Input.tsx
@components/ui/Button.tsx
@components/ui/Card.tsx
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1 (tracer): AuthFrame baru + halaman login portal token-native — satu jalur ujung-ke-ujung</name>
  <files>components/auth/AuthFrame.tsx (baru), app/portal-login/page.tsx</files>
  <read_first>
    - app/portal-login/page.tsx (file yang diubah — kontrak handleSubmit saat ini)
    - app/portal-login/layout.tsx (sumber kebenaran: basePath provider, JANGAN diubah)
    - components/ui/Input.tsx, components/ui/Button.tsx, components/ui/Card.tsx (kit yang dipakai ulang)
    - docs/superpowers/specs/2026-08-12-ui-ux-rework-design.md §7.1 (sumber desain auth frame)
    - .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-UI-SPEC.md §1 (kontrak token/type/spacing/copy yang di-lock)
  </read_first>
  <acceptance_criteria>
    - AuthFrame: `flex min-h-screen items-center justify-center bg-surface-0 px-5 py-10` shell; card `w-full max-w-[400px] rounded-sheet border border-border bg-surface-1 p-10 shadow-lvl-2`; brand tile 48px `bg-santos-red` with white "S" glyph `font-display font-semibold`; wordmark "SANTOS JAYA ABADI" `font-display font-semibold text-sm tracking-[0.1em] text-text-1`; eyebrow slot `text-xs font-semibold tracking-[0.2em] text-accent`; title `font-display text-xl font-semibold text-text-1`; subtitle `text-sm text-text-2`; error slot `role="alert"` with `border border-danger/30 bg-danger-subtle` region + WarningCircle size 20 + `text-sm text-danger`; optional footer slot rendered below the card.
    - portal-login: NIK HRIS + Password fields via kit Input with visible labels (`text-xs font-semibold text-text-2 mb-2`), placeholder "Masukkan NIK Anda" / "Password" kept; submit = kit Button primary full-width `w-full` label "Masuk" / "Masuk..." (disabled while loading); footnote `text-xs text-text-3` "Lupa password? Hubungi Admin HRIS."; AuthFrame eyebrow "PORTAL SSO", title "Masuk ke Portal".
    - Error mapping function in page: result.error === "CredentialsSignin" (or any generic provider error) → "NIK atau password salah"; known server messages ("Password salah", "NIK tidak ditemukan", "Akun dinonaktifkan. Hubungi administrator.", "Akun terkunci. Coba lagi dalam N menit.") passed through verbatim; everything else → "Terjadi kesalahan. Silakan coba lagi.".
    - Behavior diff: the signIn("portal-credentials", { nik, password, redirect: false, callbackUrl: "/portal" }) call and the router.push("/portal") + router.refresh() success path unchanged.
    - Static gates on the two files: `! grep -nE '#[0-9a-fA-F]{3,8}' <files>` (no raw hex); `! grep -n 'style=\{\{' <files>` (no inline style objects); `! grep -n 'react-icons' <files>` (no legacy icon imports); Phosphor icons only via top-level named imports.
    - Typography discipline: only sizes 12/14/20/24 (`text-xs`/`text-sm`/`text-xl`), only weights 400 (`font-normal`) and 600 (`font-semibold`) in the touched files; no arbitrary-size utilities; 4px grid only (no half-step gaps).
  </acceptance_criteria>
  <action>
    Buat komponen presentasional baru components/auth/AuthFrame.tsx (tanpa "use client" — murni tampilan). Struktur: shell luar seperti di acceptance criteria; brand block terpusat di atas card (tile persegi 48px bg-santos-red berisi monogram "S" putih font-display semibold — mark legacy dipertahankan, weight 700 dilipat ke 600), wordmark "SANTOS JAYA ABADI"; slot eyebrow opsional (diteken accent, tracking lebar, uppercase); title font-display 20px semibold; subtitle opsional 14px text-text-2; slot error opsional (region danger di atas children — bukan toast, bukan blank page); children = form; slot footer opsional dirender di bawah card (dipakai admin: "© 2024 PT. Santos Jaya Abadi"). Props: eyebrow?, title, subtitle?, error?, footer?, children. Semua warna dari token class (surface/text/border/accent/danger) — dilarang nilai hex apa pun.

    Tulis ulang app/portal-login/page.tsx hanya pada lapisan chrome: pertahankan "use client", state (nik/password/error/isLoading), dan logika handleSubmit identik — termasuk pemanggilan signIn dengan provider id, argumen, redirect:false, callbackUrl "/portal", jalur sukses router.push+refresh, dan catch yang menetapkan pesan generik. Ganti seluruh style object dengan AuthFrame + kit Input/Button. Tambahkan fungsi mapping error per acceptance criteria (pesan dari server dipertahankan verbatim; error generik provider dipetakan ke "NIK atau password salah"; fallback catch ke pesan generik). CTA submit: kit Button variant primary, full width, disabled saat loading dengan label "Masuk..." — tanpa teks ALL-CAPS (aturan sentence case). Footnote di dalam card memakai slot footer atau paragraf token-class. Jangan sentuh app/portal-login/layout.tsx (provider basePath adalah perilaku yang dibekukan). Nama identitas dan nilai class mengikuti UI-SPEC §1 secara harfiah; jangan menambah prop atau elemen di luar kontrak. Pesan commit dalam Bahasa Indonesia.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit (exit 0)
      npx eslint components/auth/AuthFrame.tsx app/portal-login/page.tsx (0 errors)
      ! grep -nE '#[0-9a-fA-F]{3,8}' components/auth/AuthFrame.tsx app/portal-login/page.tsx
      ! grep -n 'style=\{\{' components/auth/AuthFrame.tsx app/portal-login/page.tsx
      ! grep -n 'react-icons' components/auth/AuthFrame.tsx app/portal-login/page.tsx
      git diff -- app/portal-login/layout.tsx (kosong — perilaku provider utuh)
      grep -n 'portal-credentials' app/portal-login/page.tsx (pemanggilan signIn masih ada dengan provider id yang sama)
    </automated>
    <human-check>Opsional (PRE-1: env render rusak): bila env tersedia, buka /portal-login di theme night dan paper, verifikasi card terpusat + error inline + loading "Masuk...". Bila tidak, catat keterbatasan di laporan verifikasi.</human-check>
  </verify>
  <done>AuthFrame ada dan dipakai halaman portal-login; signIn/callbackUrl/redirect path identik dengan sebelumnya (diff UI-only); semua gate static + tsc + eslint hijau; error mapping mengikuti kontrak copy; komit dibuat dengan pesan Bahasa Indonesia.</done>
  <reversibility rating="reversible">Presentasi-only; bila frame bermasalah, kembalikan komponen tanpa dampak pada perilaku auth.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Halaman admin-login token-native di atas AuthFrame (panel split dibuang)</name>
  <files>app/(auth)/admin-login/page.tsx</files>
  <read_first>
    - app/(auth)/admin-login/page.tsx (file yang diubah — chrome inline + react-icons saat ini)
    - app/(auth)/layout.tsx (sumber kebenaran: basePath "/api/auth", JANGAN diubah)
    - components/auth/AuthFrame.tsx (hasil Task 1 — kontrak prop)
    - components/ui/Input.tsx, components/ui/Button.tsx (kit)
    - .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-UI-SPEC.md §1 (kontrak admin frame)
  </read_first>
  <acceptance_criteria>
    - Split-panel branding column (lebar 50% + gradient) dihapus total — D-04 "no split panels"; semua branding pindah ke AuthFrame.
    - Link "Kembali ke Beranda" (ghost, ArrowLeft size 16, `text-text-2 hover:text-text-1`) tetap di atas card.
    - AuthFrame: title "Masuk ke Admin", subtitle "Gunakan kredensial admin untuk mengakses dashboard", footer "© 2024 PT. Santos Jaya Abadi" (`text-xs text-text-3` terpusat di bawah card).
    - Field Email + Password via kit Input dengan ikon leading EnvelopeSimple / LockKey (Phosphor, size 16), label terlihat (`text-xs font-semibold text-text-2 mb-2`), placeholder "admin@example.com" / "••••••••" dipertahankan.
    - Error region: teks "Kredensial tidak valid" (pesan yang ada) dirender inline di slot error AuthFrame — bukan toast; catch tetap "Terjadi kesalahan. Silakan coba lagi.".
    - CTA: kit Button primary full-width "Masuk" / "Masuk..." (sentence case; teks ALL-CAPS "MASUK"/"MEMPROSES..." tidak dipakai).
    - Behavior diff: signIn("credentials", { email, password, redirect: false }) dan router.push("/admin") tidak berubah; `signIn` tetap dari next-auth/react.
    - Static gates pada file: `! grep -nE '#[0-9a-fA-F]{3,8}' <file>`; `! grep -n 'style=\{\{' <file>`; `! grep -n 'react-icons' <file>`; hanya 4 ukuran type (12/14/20/24) + 2 weight (400/600).
  </acceptance_criteria>
  <action>
    Tulis ulang app/(auth)/admin-login/page.tsx memakai AuthFrame yang dibuat Task 1. Buang seluruh chrome inline (background, gradient branding column, style object form/button/label) dan seluruh ikon line-style — ganti dengan kit Input/Button dan ikon Phosphor EnvelopeSimple + LockKey (import named top-level, weight regular, size 16) untuk leading icon di field. Susun: link "Kembali ke Beranda" (next/link, ghost style token, ArrowLeft 16) di atas card; AuthFrame dengan title/subtitle/footer sesuai acceptance criteria; form berisi dua kit Input (label terlihat) dan CTA kit Button primary full-width dengan label loading "Masuk...". Pertahankan state, handleSubmit, mapping error ("Kredensial tidak valid" untuk result.error; catch ke pesan generik), dan jalur sukses router.push("/admin") secara identik. Jangan ubah app/(auth)/layout.tsx. Jangan menambah fitur (tanpa link lupa password admin, tanpa remember-me — tidak ada di kontrak). Pesan commit dalam Bahasa Indonesia.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit (exit 0)
      npx eslint "app/(auth)/admin-login/page.tsx" (0 errors)
      ! grep -nE '#[0-9a-fA-F]{3,8}' "app/(auth)/admin-login/page.tsx"
      ! grep -n 'style=\{\{' "app/(auth)/admin-login/page.tsx"
      ! grep -n 'react-icons' "app/(auth)/admin-login/page.tsx"
      git diff -- "app/(auth)/layout.tsx" (kosong)
      grep -n '"credentials"' "app/(auth)/admin-login/page.tsx" (provider id dipertahankan)
    </automated>
  </verify>
  <done>admin-login dirender token-native dengan AuthFrame tanpa panel split; signIn("credentials") dan redirect ke /admin identik; gate tsc/eslint/grep hijau; komit dibuat dengan pesan Bahasa Indonesia.</done>
  <reversibility rating="reversible">Presentasi-only; panel split lama dapat dikembalikan dari git bila diperlukan.</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces (plan 11-01)

| Symbol / File | Kind | Used By |
|---------------|------|---------|
| `components/auth/AuthFrame.tsx` | NEW component (presentational, shared) | portal-login, admin-login (this plan); future auth surfaces |
| `app/portal-login/page.tsx` | RESTYLED (chrome only, AuthFrame + error mapping) | /portal-login route |
| `app/(auth)/admin-login/page.tsx` | RESTYLED (chrome only, split panel removed) | /admin-login route |

No new deps, no new routes, no lib/ changes. `lib/portal-auth.ts`, `lib/auth.ts`, `middleware.ts`, both auth `layout.tsx` files untouched.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (login form) → NextAuth provider | Untrusted credentials cross here; unchanged by this plan — only the frame/chrome changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Spoofing | portal-login error mapping | low | accept | Mapping only aliases the generic CredentialsSignin error to "NIK atau password salah"; server messages (already shipped in frozen lib/portal-auth.ts) pass through verbatim — no new enumeration surface introduced |
| T-11-02 | Information disclosure | AuthFrame rendering | low | accept | Frame renders only static brand copy + server-provided error text; no user/account data reaches the frame |
| T-11-03 | Tampering | signIn call sites | medium | mitigate | Task actions + diffs assert signIn provider ids, callbackUrl, and cookie-prefix-bearing layouts byte-identical; git-diff gate on both layout.tsx files |
| T-11-04 | Tampering | inline-style removal | low | mitigate | Static greps ban raw hex, inline style objects, and legacy icon imports in touched files (D-08) |
| T-11-SC | Tampering | npm installs | low | accept | No package installs in this plan; zero new dependencies by design (UIUX-01 "no new deps") |
</threat_model>

<verification>
- `npx tsc --noEmit` exit 0 (whole project).
- Scoped `npx eslint` on the two pages + AuthFrame: 0 errors (pre-existing warnings allowed).
- Static grep per D-08 on the three touched files: 0 raw hex, 0 inline style objects, 0 legacy icon imports.
- Behavior-preservation diffs: `app/portal-login/layout.tsx` and `app/(auth)/layout.tsx` show zero diff; signIn provider ids/callbackUrl lines unchanged (grep-verified).
- Screenshots (both themes, /portal-login + /admin-login) = bonus only if an env can render (PRE-1); otherwise the limitation is documented in the verification report.
</verification>

<success_criteria>
- Both login pages render the shared centered-card auth frame from tokens (D-04); no split panels remain; brand mark + app name + inline error present on both.
- Login behavior identical: same NextAuth provider ids, same cookie prefixes (portal-auth.* / next-auth.*), same redirects, same audit events (SPEC R1 acceptance; D-05).
- Grep of the three touched files: 0 legacy icon imports, 0 raw hex, 0 inline style objects; Phosphor + tokens everywhere (SPEC R3 acceptance pattern).
- `npx tsc --noEmit` exit 0 and scoped eslint 0 errors (SPEC R5).
- OPD-1 recheck: `git diff -- lib/portal-auth.ts lib/auth.ts middleware.ts` empty.
</success_criteria>

<output>
Create `.planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-01-SUMMARY.md` when done
</output>
