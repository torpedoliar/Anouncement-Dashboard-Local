---
phase: 11-portal-auth-surfaces
plan: 03
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - components/portal/AccessDenied.tsx
  - components/portal/NoCredential.tsx
  - components/portal/CorruptCredential.tsx
  - components/portal/AccountSelector.tsx
  - components/portal/SSOAutoSubmit.tsx
  - components/portal/SSORerouteSubmit.tsx
  - components/portal/SSOCredentialVault.tsx
  - components/portal/OnboardingWizard.tsx
  - components/portal/VisibilitySettings.tsx
  - app/portal/settings/page.tsx
  - app/portal/credentials/page.tsx
autonomous: true
requirements:
  - UIUX-05
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Failure/denied surfaces share one centered-sheet layout (shell flex min-h-screen items-center justify-center bg-surface-0 px-5 py-10; content text-center max-w-[400px]): 56px rounded-sheet tinted tile bg-warning-subtle border-warning/30 for NoCredential (WarningCircle 24) and CorruptCredential (WarningTriangle 24), bg-danger-subtle border-danger/30 for AccessDenied (ShieldWarning 24); title font-display text-xl font-semibold; body text-sm text-text-2; CTA hrefs unchanged (Simpan Kredensial → /portal/credentials?app=, Simpan Ulang Kredensial → same, Kembali ke Portal → /portal); copy kept per UI-SPEC (SPEC R3, UI consideration error)."
    - "SSO interstitial sheets (SSOAutoSubmit, SSORerouteSubmit) restyled token-only: icon tile + title 'SSO ke {app.name}' / 'SSO ke {app.name} (Reroute)' + status copy ('Menyiapkan sesi dan kredensial...' / 'Mengalihkan...' and 'Menyiapkan sesi server-to-server...'; 'Mengisi kredensial...' / 'Selesai!') + animate-spin loader using token borders; the hidden auto-submit form (method, action, field names/values, id sso-form / sso-reroute-form) is BYTE-IDENTICAL and the 1.5s submit timer logic is unchanged (SPEC R5, prohibition tampering)."
    - "SSOCredentialVault restyled token-only: app section header (logo 56px rounded-sheet object-cover or initial tile, name font-display text-xl font-semibold, LockKey 12 hint 'Kredensial tersimpan (mode Vault)' text-xs text-text-3); username/password rows as rounded-control border-border bg-surface-0 mono inputs (font-mono tabular-nums); ghost icon buttons Copy/Check (copied flash)/Eye/EyeSlash with aria-labels; 'Buka Halaman Login {app.name}' button keeps window.open(app.url, '_blank', 'noopener,noreferrer'); the execCommand clipboard fallback for HTTP internal deploys is KEPT; no new actions added (no delete button — delete lives only in the credentials page)."
    - "OnboardingWizard restyled single-screen (UI-SPEC UI consideration ⚠ UNRESOLVED — explicit assumption: no step navigation invented, PORT-13 frozen): layout p-8 max-w-[900px] mx-auto; title font-display text-xl; body text-sm text-text-2; per-group kit Card p-4 with header flex items-center gap-2 font-semibold text-text-1 + native checkbox accent-accent; app rows pl-7 py-1 text-sm text-text-2; empty group row 'Tidak ada aplikasi dalam grup ini.' text-xs text-text-3; footer Simpan (primary, 'Menyimpan...' while saving) + Lewati (secondary, onboarding mode only); the POST /api/portal/visibility body construction (groupIdsOff/appIdsOff/appIdsOn/skip merge rules for settings mode) and window.location.href = '/portal' redirect are UNCHANGED."
    - "Credentials page restyled token-only: header (eyebrow 'PORTAL SSO', H1 'Kredensial' font-display text-2xl, sub 'Simpan satu atau beberapa akun untuk setiap aplikasi. Kredensial disimpan terenkripsi.' text-sm text-text-2); per-app accordion kit Cards with account rows (label + 'Terakhir dipakai ...' mono), add-account form via kit Input (Label Akun/Username/Password) + kit Button (Plus icon, 'Tambah Akun'/'Menyimpan...'), delete via kit ConfirmDialog with existing copy 'Hapus Akun' (danger variant); password NEVER prefilled (form state starts empty — PORT-08 behavior kept); ?app= auto-expand behavior kept; empty state icon-tile card 'Belum ada aplikasi yang di-assign ke Anda.'; fetch URLs and payload shapes unchanged."
    - "Settings page restyled token-only: page shell (eyebrow 'PORTAL SSO', H1 'Pengaturan' font-display text-2xl) + VisibilitySettings reused in settings mode; explicit assumption (repo ground truth): the settings page currently carries ONLY the visibility toggles — change-password / own-sessions sections do not exist client-side today and are NOT added (no new capabilities guardrail)."
  artifacts:
    - components/portal/AccessDenied.tsx, NoCredential.tsx, CorruptCredential.tsx, AccountSelector.tsx (centered-sheet family)
    - components/portal/SSOAutoSubmit.tsx, SSORerouteSubmit.tsx, SSOCredentialVault.tsx (interstitials + vault)
    - components/portal/OnboardingWizard.tsx, VisibilitySettings.tsx, app/portal/settings/page.tsx, app/portal/credentials/page.tsx (settings + credentials flows)
  key_links:
    - /portal/app/[appSlug] page (server, untouched) renders AccessDenied/NoCredential/CorruptCredential/AccountSelector/SSO interstitials/vault — props contracts unchanged
    - SSO auto-submit form fields feed the target app login — byte-identical requirement
    - Visibility POST body construction + redirect — unchanged (PORT-13)
  prohibitions:
    - statement: MUST NOT alter the SSO auto-submit forms (method, action, field names/values, target) or the 1.5s submit timer logic in SSOAutoSubmit/SSORerouteSubmit
      status: resolved
      verification: diff on the two form elements byte-identical; timer logic lines unchanged
    - statement: MUST NOT change visibility/credential API shapes or the POST /api/portal/visibility body construction (PORT-13 frozen)
      status: resolved
      verification: judgment — diff on wizard fetch/body lines unchanged
    - statement: MUST NOT prefill password fields on load (PORT-08)
      status: resolved
      verification: judgment — form state initializes empty in credentials page; grep confirms no defaultValue/value injection from fetched data for password
    - statement: MUST NOT introduce legacy icon imports, inline style objects, raw hex, or new capabilities (no delete button in vault, no new settings sections)
      status: resolved
      verification: static scoped grep per D-08
  backstops:
    - statement: Interstitial/vault sheets keep 1.5s visible credential-injection feedback before auto-submit (existing UX)
      verification: backstop — held-out visual check when an env can render; otherwise documented
  assumptions:
    - "OnboardingWizard stays single-screen (no step navigation): the shipped wizard is a single checklist posting one visibility replace; inventing steps would change the frozen POST contract (UI-SPEC ⚠ unresolved → assumption)."
    - "Settings page scope = visibility toggles only: repo ground truth shows app/portal/settings/page.tsx renders only VisibilitySettings; change-password/session sections are NOT added (no new capabilities)."
---

<objective>
Portal SSO secondary surfaces — failure states, interstitials, vault, wizard, settings, credentials.

Purpose: Closes SPEC R3 — every remaining portal surface token-native with zero chrome, while SSO launch behavior (auto-submit forms, timer, audits, failure rendering) and the visibility/credential API contracts stay frozen. This is the plan where the phase's assumption for the wizard (single-screen, no invented step nav) is applied explicitly.

Output: 11 restyled files across three cohesive groups: centered-sheet failure/denied family (+ account selector), SSO interstitials + credential vault, and the wizard/settings/credentials flows.

Frozen behavior: SSO auto-submit forms and timers, audit calls, visibility POST construction, credential API shapes, password never prefilled, `?app=` auto-expand. Gates per D-08; screenshots bonus (PRE-1).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-CONTEXT.md
@.planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-SPEC.md
@.planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-UI-SPEC.md
@app/portal/app/[appSlug]/page.tsx
@app/portal/credentials/page.tsx
@app/portal/settings/page.tsx
@components/portal/AccessDenied.tsx
@components/portal/NoCredential.tsx
@components/portal/CorruptCredential.tsx
@components/portal/AccountSelector.tsx
@components/portal/SSOAutoSubmit.tsx
@components/portal/SSORerouteSubmit.tsx
@components/portal/SSOCredentialVault.tsx
@components/portal/OnboardingWizard.tsx
@components/portal/VisibilitySettings.tsx
@components/ui/Card.tsx
@components/ui/Button.tsx
@components/ui/Input.tsx
@components/ui/ConfirmDialog.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Family sheet terpusat — AccessDenied, NoCredential, CorruptCredential, AccountSelector</name>
  <files>components/portal/AccessDenied.tsx, components/portal/NoCredential.tsx, components/portal/CorruptCredential.tsx, components/portal/AccountSelector.tsx</files>
  <read_first>
    - components/portal/AccessDenied.tsx, NoCredential.tsx, CorruptCredential.tsx, AccountSelector.tsx (file yang diubah)
    - .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-UI-SPEC.md §4 (kontrak sheet + copy)
    - app/portal/app/[appSlug]/page.tsx (pemanggil — kontrak props TIDAK berubah)
  </read_first>
  <acceptance_criteria>
    - Tiga state gagal memakai shell bersama: `flex min-h-screen items-center justify-center bg-surface-0 px-5 py-10`; konten `text-center max-w-[400px]`; tile 56px `rounded-sheet` dengan border/icon sesuai semantik — NoCredential: `bg-warning-subtle border-warning/30` + WarningCircle 24; CorruptCredential: `bg-warning-subtle border-warning/30` + WarningTriangle 24; AccessDenied: `bg-danger-subtle border-danger/30` + ShieldWarning 24; title `font-display text-xl font-semibold text-text-1`; body `text-sm text-text-2` (line-height 1.6 dari globals); CTA kit Button/primary atau secondary dengan href IDENTIK (No/Corrupt → `/portal/credentials?app=${appSlug}`; AccessDenied → `/portal`).
    - Copy dipertahankan verbatim: "Kredensial Belum Disimpan" / "Anda belum menyimpan kredensial untuk {appName}" / "Simpan Kredensial"; "Kredensial Rusak" / "Kredensial untuk {appName} tidak dapat dibaca. Silakan simpan ulang untuk melanjutkan." / "Simpan Ulang Kredensial"; "Akses Ditolak" / "Anda tidak punya akses ke {appName}" / "Kembali ke Portal".
    - AccountSelector: sheet terpusat (bukan p-32 top-aligned) dengan title "Pilih Akun" font-display text-xl + body "Aplikasi {appName} memiliki lebih dari satu akun tersimpan. Pilih akun yang ingin digunakan." text-sm text-text-2; row akun `flex w-full items-center justify-between gap-3 rounded-control border border-border bg-surface-1 px-4 py-3 text-sm hover:bg-surface-2`; label `font-semibold text-text-1` + sub mono `font-mono tabular-nums text-xs text-text-3`; setiap row = link ke `${baseHref}?credentialId=${a.id}` (mekanisme pilihan TIDAK berubah); tombol "Batal" ghost (kit Button variant ghost, next/link ke /portal) ditambahkan per kontrak copy "Batal (selector/dialogs)".
    - Static gates keempat file: `! grep -nE '#[0-9a-fA-F]{3,8}' <files>`; `! grep -n 'style=\{\{' <files>`; `! grep -n 'react-icons' <files>`; hanya size 12/14/20/24 + weight 400/600; tanpa utility setengah-grid.
  </acceptance_criteria>
  <action>
    Tulis ulang keempat komponen pada lapisan visual sesuai kontrak UI-SPEC §4. Buang seluruh style object dan ikon line-style; ganti dengan class token + ikon Phosphor (import named top-level: WarningCircle, WarningTriangle, ShieldWarning, Check — ukuran sesuai acceptance criteria). Pertahankan props interface, "use client" (bila ada), href, dan seluruh teks copy verbatim. Untuk AccountSelector: ubah tata letak dari top-aligned menjadi sheet terpusat, pertahankan link href `?credentialId=` per akun, tambahkan aksi batal ghost ke /portal (satu-satunya tambahan — diizinkan kontrak copy "Batal"). Jangan menambah state, jangan mengubah logika pilihan. Pesan commit dalam Bahasa Indonesia.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit (exit 0)
      npx eslint components/portal/AccessDenied.tsx components/portal/NoCredential.tsx components/portal/CorruptCredential.tsx components/portal/AccountSelector.tsx (0 errors)
      ! grep -nE '#[0-9a-fA-F]{3,8}' components/portal/AccessDenied.tsx components/portal/NoCredential.tsx components/portal/CorruptCredential.tsx components/portal/AccountSelector.tsx
      ! grep -n 'style=\{\{' components/portal/AccessDenied.tsx components/portal/NoCredential.tsx components/portal/CorruptCredential.tsx components/portal/AccountSelector.tsx
      ! grep -n 'react-icons' components/portal/AccessDenied.tsx components/portal/NoCredential.tsx components/portal/CorruptCredential.tsx components/portal/AccountSelector.tsx
      grep -n 'credentialId=' components/portal/AccountSelector.tsx (mekanisme pemilihan dipertahankan)
    </automated>
  </verify>
  <done>Keempat komponen token-native dengan copy verbatim, href dan mekanisme credentialId identik; gate hijau; komit Bahasa Indonesia.</done>
  <reversibility rating="reversible">Presentasi-only.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Interstisial SSO + vault kredensial — form auto-submit utuh (R5)</name>
  <files>components/portal/SSOAutoSubmit.tsx, components/portal/SSORerouteSubmit.tsx, components/portal/SSOCredentialVault.tsx</files>
  <read_first>
    - components/portal/SSOAutoSubmit.tsx, SSORerouteSubmit.tsx, SSOCredentialVault.tsx (file yang diubah)
    - app/portal/app/[appSlug]/page.tsx (pemanggil — kontrak props TIDAK berubah)
    - .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-UI-SPEC.md §4 (kontrak interstisial + vault)
    - docs/specs/04-sso-credential-forwarding.md (perilaku SSO yang dibekukan)
  </read_first>
  <acceptance_criteria>
    - SSOAutoSubmit/SSORerouteSubmit: sheet terpusat token-native (icon tile + title + status copy); spinner `animate-spin` memakai border token (`border-border border-t-accent` style atau setara token-class) MENGGANTI keyframes inline `<style>`; form tersembunyi (id sso-form / sso-reroute-form, method, action, input hidden name/value, credentialId untuk reroute) BYTE-IDENTICAL; useEffect timer 1500ms + formRef.submit() TIDAK berubah; teks "Mengisi kredensial..."/"Mengalihkan..."/"Selesai!" (SSOAutoSubmit) dan "Menghubungi server target..."/"Mengalihkan..."/"Selesai!" (SSORerouteSubmit) dipertahankan.
    - SSOCredentialVault: header app (logo 56px rounded-sheet object-cover atau initial tile `bg-surface-3 text-text-2`, name font-display text-xl font-semibold, LockKey 12 + "Kredensial tersimpan (mode Vault)" text-xs text-text-3); input readonly username/password `rounded-control border border-border bg-surface-0 font-mono tabular-nums` (password letterSpacing saat tersembunyi dipertahankan); tombol ghost 42px (size ikon 16) dengan aria-label "Salin username"/"Salin password"/"Lihat password"/"Sembunyikan password"; flash "ter-salin" dengan Check berwarna success; fungsi copyText (clipboard API + fallback execCommand untuk HTTP internal) dan openTarget window.open(app.url, '_blank', 'noopener,noreferrer') TIDAK diubah; tidak ada tombol hapus baru.
    - Pemetaan ikon per UI-SPEC: FiCopy→Copy, FiCheck→Check, FiEye→Eye, FiEyeOff→EyeSlash, FiExternalLink→ExternalLink, FiLock→LockKey (semua Phosphor named top-level).
    - Static gates ketiga file: `! grep -nE '#[0-9a-fA-F]{3,8}' <files>`; `! grep -n 'style=\{\{' <files>`; `! grep -n 'react-icons' <files>`; tanpa utility setengah-grid.
  </acceptance_criteria>
  <action>
    Tulis ulang chrome ketiga komponen sesuai kontrak UI-SPEC §4. SSOAutoSubmit/SSORerouteSubmit: ganti shell inline dan keyframes spinner (definisi keyframes lokal dihapus; pakai utility animate-spin bawaan Tailwind dengan border token), ganti ikon line-style dengan Phosphor (CheckCircle dll. named top-level), pertahankan seluruh logika timer/ref/submit dan markup form tersembunyi TANPA SATU PERUBAHAN (bandingkan baris form sebelum/sesudah via git diff — harus identik byte-per-byte). SSOCredentialVault: pertahankan fungsi copyText (termasuk fallback execCommand dan komentar konteks HTTP internal) serta openTarget, ganti seluruh style object dengan class token, ganti ikon sesuai peta pemetaan, pertahankan label USERNAME/PASSWORD (sentence case, bukan ALL-CAPS — label kini `text-xs font-semibold text-text-2`), jangan tambahkan interaksi baru (tidak ada hapus di vault — penghapusan hanya ada di halaman kredensial). Pesan commit dalam Bahasa Indonesia.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit (exit 0)
      npx eslint components/portal/SSOAutoSubmit.tsx components/portal/SSORerouteSubmit.tsx components/portal/SSOCredentialVault.tsx (0 errors)
      ! grep -nE '#[0-9a-fA-F]{3,8}' components/portal/SSOAutoSubmit.tsx components/portal/SSORerouteSubmit.tsx components/portal/SSOCredentialVault.tsx
      ! grep -n 'style=\{\{' components/portal/SSOAutoSubmit.tsx components/portal/SSORerouteSubmit.tsx components/portal/SSOCredentialVault.tsx
      ! grep -n 'react-icons' components/portal/SSOAutoSubmit.tsx components/portal/SSORerouteSubmit.tsx components/portal/SSOCredentialVault.tsx
      grep -c 'method=\|action=\|type="hidden"' components/portal/SSOAutoSubmit.tsx (>= 1 — form tersembunyi baris 139-147 tetap ada; measured: 5)
      grep -c 'method=\|action=\|type="hidden"' components/portal/SSORerouteSubmit.tsx (>= 1 — form tersembunyi baris 135-141 tetap ada; measured: 4)
      ! git diff -- components/portal/SSOAutoSubmit.tsx components/portal/SSORerouteSubmit.tsx | grep -nE '^[+-].*(method=|action=|type="hidden")' (MUST print nothing — diff atas wilayah kedua form kosong = byte-identical)
      grep -c 'formRef.current.submit()' components/portal/SSOAutoSubmit.tsx components/portal/SSORerouteSubmit.tsx (masing-masing >= 1; measured: 1 each)
    </automated>
  </verify>
  <done>Interstisial dan vault token-native; form auto-submit byte-identical; timer dan fungsi copy/openTarget utuh; gate hijau; komit Bahasa Indonesia.</done>
  <reversibility rating="reversible">Presentasi-only; form tersembunyi menjadi titik kritis — diff form kosong wajib sebelum komit.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Alur wizard/settings/kredensial — OnboardingWizard, VisibilitySettings, settings, credentials</name>
  <files>components/portal/OnboardingWizard.tsx, components/portal/VisibilitySettings.tsx, app/portal/settings/page.tsx, app/portal/credentials/page.tsx</files>
  <read_first>
    - components/portal/OnboardingWizard.tsx, VisibilitySettings.tsx (file yang diubah — logika POST visibility TIDAK berubah)
    - app/portal/settings/page.tsx, app/portal/credentials/page.tsx (file yang diubah — pipeline fetch TIDAK berubah)
    - .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-UI-SPEC.md §4 (kontrak wizard/vault/halaman)
    - docs/superpowers/specs/2026-08-11-portal-user-app-visibility-design.md (PORT-13 — kontrak POST)
    - app/portal/app/[appSlug]/page.tsx (konteks vault/halaman kredensial)
  </read_first>
  <acceptance_criteria>
    - OnboardingWizard: layout `p-8 max-w-[900px] mx-auto`; title `font-display text-xl font-semibold` ("Pilih Aplikasi Anda" / "Pengaturan Aplikasi"); body `text-sm text-text-2` (kedua kalimat body dipertahankan verbatim); per grup kit Card `p-4` dengan header `flex items-center gap-2 font-semibold text-text-1` + checkbox native `accent-accent` (checked = tidak tersembunyi); row app `pl-7 py-1 text-sm text-text-2` dengan checkbox sendiri; grup kosong → "Tidak ada aplikasi dalam grup ini." `text-xs text-text-3`; footer: Simpan (kit Button primary, label "Simpan"/"Menyimpan...") + Lewati (kit Button secondary, HANYA mode onboarding); konstruksi body fetch (groupIdsOff/appIdsOff/appIdsOn/skip + merge rules settings) dan window.location.href = "/portal" TIDAK berubah; single-screen dipertahankan (tanpa step navigation — asumsi UI-SPEC ⚠).
    - VisibilitySettings: komponen pass-through TIDAK berubah isi logika; hanya pastikan class shell-nya (bila ada) token-native.
    - settings/page.tsx: shell `p-8 max-w-[900px] mx-auto` (atau setara token), eyebrow "PORTAL SSO" `text-xs font-semibold tracking-[0.2em] text-accent` + H1 "Pengaturan" `font-display text-2xl font-semibold`; render VisibilitySettings mode settings; TIDAK menambah section change-password/sessions (asumsi: tidak ada di repo saat ini — guardrail tanpa kapabilitas baru).
    - credentials/page.tsx: header (eyebrow "PORTAL SSO" + H1 "Kredensial" font-display text-2xl + sub text-sm text-text-2 verbatim); accordion per app = kit Card dengan header klik (CaretDown/ChevronDown Phosphor 16, rotasi saat expand, `transition-transform duration-200`); row akun `rounded-control border border-border bg-surface-0 px-4 py-3` dengan label font-semibold + "Terakhir dipakai ..." mono text-xs text-text-3; tombol hapus danger ghost + Trash 16 → kit ConfirmDialog (existing copy "Hapus Akun", danger variant); form tambah akun via kit Input (Label Akun/Username/Password — password TIDAK pernah terisi otomatis, state form dimulai kosong) + kit Button iconLeft Plus "Tambah Akun"/"Menyimpan..."; empty state icon-tile card "Belum ada aplikasi yang di-assign ke Anda." (Key 24 dalam tile); fetch URL "/api/portal/credentials" (GET/POST/DELETE ?credentialId=) dan payload TIDAK berubah; ?app= auto-expand TIDAK berubah; pemetaan ikon per UI-SPEC (FloppyDisk untuk simpan, Trash, Plus, CaretDown, Copy/Check/Eye/EyeSlash di baris vault bila dipakai).
    - Static gates semua file plan ini: `! grep -nE '#[0-9a-fA-F]{3,8}' <files>`; `! grep -n 'style=\{\{' <files>`; `! grep -n 'react-icons' <files>`; tanpa utility setengah-grid.
  </acceptance_criteria>
  <action>
    Tulis ulang chrome OnboardingWizard sesuai kontrak: pertahankan "use client", interface WizardApp/WizardGroup, state hiddenGroups/hiddenApps, kedua toggle, dan fungsi submit (termasuk merge initialHidden untuk mode settings) PERSIS — jangan mengubah satu baris pun dari konstruksi body POST maupun redirect. Ganti seluruh style object dengan kit Card/Button + class token; checkbox native memakai class accent-accent; hapus class warna teks hardcoded. Settings page: ganti style object shell dengan class token + header sesuai acceptance criteria; jangan menambah section. VisibilitySettings: verifikasi tidak mengandung chrome inline; bila ada, rapikan. Halaman kredensial: tulis ulang chrome (accordion, rows, form, empty state) memakai kit Input/Button/ConfirmDialog + Phosphor sesuai peta pemetaan; pertahankan seluruh logika fetch (fetchCredentials/handleSave/executeDelete), payload POST {appId,label,username,password}, DELETE ?credentialId=, state form kosong per app, auto-expand dari searchParam app, dan toast existing (useToast). Jangan menambah fitur. Pesan commit dalam Bahasa Indonesia.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit (exit 0)
      npx eslint components/portal/OnboardingWizard.tsx components/portal/VisibilitySettings.tsx app/portal/settings/page.tsx app/portal/credentials/page.tsx (0 errors)
      ! grep -nE '#[0-9a-fA-F]{3,8}' components/portal/OnboardingWizard.tsx components/portal/VisibilitySettings.tsx app/portal/settings/page.tsx app/portal/credentials/page.tsx
      ! grep -n 'style=\{\{' components/portal/OnboardingWizard.tsx components/portal/VisibilitySettings.tsx app/portal/settings/page.tsx app/portal/credentials/page.tsx
      ! grep -n 'react-icons' components/portal/OnboardingWizard.tsx components/portal/VisibilitySettings.tsx app/portal/settings/page.tsx app/portal/credentials/page.tsx
      grep -c 'groupIdsOff' components/portal/OnboardingWizard.tsx (>= 1 — konstruksi body POST dipertahankan; measured: 2)
      grep -c '/api/portal/credentials' app/portal/credentials/page.tsx (>= 3 — GET/POST/DELETE dipertahankan; measured: 3)
      grep -c 'window.location.href' components/portal/OnboardingWizard.tsx (>= 1 — redirect dipertahankan; measured: 1)
    </automated>
  </verify>
  <done>Wizard/settings/credentials token-native; POST visibility dan API kredensial identik; password tidak pernah terisi otomatis; gate hijau; komit Bahasa Indonesia.</done>
  <reversibility rating="reversible">Presentasi-only; body POST adalah titik kritis — diff baris fetch wajib kosong.</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces (plan 11-03)

| Symbol / File | Kind | Used By |
|---------------|------|---------|
| `components/portal/AccessDenied.tsx` | RESTYLED (centered sheet) | /portal/app/[appSlug] |
| `components/portal/NoCredential.tsx` | RESTYLED (centered sheet) | /portal/app/[appSlug] |
| `components/portal/CorruptCredential.tsx` | RESTYLED (centered sheet) | /portal/app/[appSlug] |
| `components/portal/AccountSelector.tsx` | RESTYLED (centered sheet + Batal) | /portal/app/[appSlug] |
| `components/portal/SSOAutoSubmit.tsx` | RESTYLED (form byte-identical) | /portal/app/[appSlug] |
| `components/portal/SSORerouteSubmit.tsx` | RESTYLED (form byte-identical) | /portal/app/[appSlug] |
| `components/portal/SSOCredentialVault.tsx` | RESTYLED (mono inputs, copy/eye) | /portal/app/[appSlug] (VAULT mode) |
| `components/portal/OnboardingWizard.tsx` | RESTYLED (single-screen kept) | /portal (onboarding), settings (mode settings) |
| `components/portal/VisibilitySettings.tsx` | PASS-THROUGH (verified) | /portal/settings |
| `app/portal/settings/page.tsx` | RESTYLED (shell + header) | /portal/settings |
| `app/portal/credentials/page.tsx` | RESTYLED (accordion + form) | /portal/credentials |

No new components/routes/APIs. `lib/portal-access.ts` / `lib/portal-auth.ts` / `lib/portal-crypto.ts` untouched.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSO launch page → external app login | Stored credentials POST cross-origin via auto-submit form; form markup frozen |
| credentials page → /api/portal/credentials | Credential CRUD API; payload shapes frozen |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-09 | Tampering | SSOAutoSubmit/SSORerouteSubmit hidden forms | critical | mitigate | Form elements must be byte-identical (method/action/fields/ids); task gate diffs the form region; timer logic untouched — a tampered form would change where credentials POST |
| T-11-10 | Information disclosure | SSOCredentialVault mono inputs | medium | mitigate | Password input keeps type=password + reveal toggle + letter-spacing masking; no value ever rendered into non-input DOM (React text nodes only); copy uses existing clipboard path |
| T-11-11 | Information disclosure | AccountSelector rows | low | accept | Rows expose only credential labels (already the case); selection mechanism (credentialId link) unchanged |
| T-11-12 | Tampering | visibility POST construction | high | mitigate | Wizard diff gate asserts groupIdsOff/appIdsOff/appIdsOn/skip merge rules byte-identical (PORT-13 frozen) |
| T-11-13 | Tampering | credentials API payload | high | mitigate | fetch URLs, payload {appId,label,username,password}, and DELETE credentialId param unchanged (grep-gated) |
| T-11-14 | XSS | app name strings in failure/sheet copy | low | accept | Rendered via React text nodes; no dangerouslySetInnerHTML introduced |
| T-11-SC | Tampering | npm installs | low | accept | No package installs in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` exit 0; scoped eslint 0 errors on all 11 touched files.
- Static grep on touched files: 0 raw hex, 0 inline style objects, 0 legacy icon imports.
- Byte-identity greps: hidden form fields + formRef.submit() present in both interstitial components; groupIdsOff + window.location.href present in wizard; /api/portal/credentials present in credentials page.
- OPD-1/OPD-4: frozen lib files untouched (git diff empty); no stale compound-key lookups introduced in touched code.
- Screenshots (both themes) = bonus (PRE-1); otherwise documented limitation.
</verification>

<success_criteria>
- All portal secondary surfaces render token-native with zero chrome (SPEC R3 acceptance grep).
- SSO launch behavior proven unchanged: auto-submit forms byte-identical, failure states render + audit (server page untouched), account picker path intact (SPEC R2).
- Wizard stays single-screen; visibility and credential API contracts frozen (PORT-13/PORT-08).
- `npx tsc --noEmit` exit 0 + scoped eslint 0 errors (SPEC R5).
</success_criteria>

<output>
Create `.planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-03-SUMMARY.md` when done
</output>
