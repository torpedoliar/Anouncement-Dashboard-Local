---
phase: 11-portal-auth-surfaces
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - app/portal/layout.tsx
  - components/portal/PortalHeader.tsx
  - app/portal/page.tsx
  - components/portal/GroupedAppGrid.tsx
  - components/portal/AppCard.tsx
autonomous: true
requirements:
  - UIUX-05
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Portal shell is token-native: app/portal/layout.tsx renders min-h-screen bg-surface-0 text-text-1 (no inline color), and the session guard redirect('/portal-login') is untouched (SPEC R3)."
    - "PortalHeader is a sticky token topbar: border-b border-border bg-surface-1, h-14, wordmark with 'PORTAL' in accent, nav Aplikasi/Kredensial/Pengaturan with Phosphor 16px icons, active item bg-accent-subtle text-accent with aria-current, inactive text-text-2 hover:bg-surface-2; Keluar = kit Button secondary sm + SignOut 16 calling signOut({ callbackUrl: '/portal-login' }) UNCHANGED; mobile List/X toggle with slide-down panel (rows min-h-11) — no horizontal scroll at 375px (UI-SPEC §2, UI consideration overflow)."
    - "Grid is RESTRUCTURED per D-01: category chip row ('Semua' + one chip per group name from `groups`, aria-pressed, client-side selection state, no fetch/URL change) above a responsive grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 (base 1-col keeps 375px overflow-free); app sort order inside groups preserved (server displayOrder/name, no client re-sort)."
    - "Data pipeline in app/portal/page.tsx is 100% unchanged (SPEC R2 / OPD-1): getPortalLayout → wizard gate → getAccessiblePortalApps → credential groupBy → gridGroups; GridGroup/GridApp prop contract intact (D-03)."
    - "Empty states per locked copy (UI-SPEC, resolves SPEC edge R2-empty + D-02): zero accessible apps → 'Belum ada aplikasi' heading + 'Tidak ada aplikasi yang dapat ditampilkan saat ini. Atur visibilitas lewat Pengaturan.' body + 'Buka Pengaturan' button to /portal/settings (SquaresFour 24 in tinted tile); chip-filtered-to-empty → same card with 'Belum ada aplikasi di grup ini.'."
    - "AppCard is a kit launch card (D-03 visual layer only): Card p-6 with hover raise (transition-all duration-150 hover:-translate-y-0.5 hover:shadow-lvl-2) + focus-within outline-accent; logo 40px rounded-card or initial tile bg-surface-3 text-text-2; name text-sm font-semibold truncate; category text-xs text-text-3; description text-sm text-text-2 line-clamp-2; health row text-xs with CheckCircle success '{N} akun tersimpan' (mono digit) or WarningCircle warning 'Belum ada akun'; CTA credentialCount>0 → primary 'Buka' (block w-full rounded-control bg-accent py-2 text-center text-sm font-semibold text-white hover:opacity-90, Link to /portal/app/{slug} target _blank rel noopener noreferrer) else secondary 'Simpan Kredensial' (Link to /portal/credentials?app={slug}); alt={name} on img; keyboard order = visual (single focusable = CTA)."
  artifacts:
    - app/portal/layout.tsx (token shell, guard untouched)
    - components/portal/PortalHeader.tsx (token topbar + mobile panel)
    - app/portal/page.tsx (token header + empty states, pipeline untouched)
    - components/portal/GroupedAppGrid.tsx (chip row + responsive grid + filtered empty)
    - components/portal/AppCard.tsx (kit launch card)
  key_links:
    - GroupedAppGrid props (GridGroup[] with apps + credentialCount) consumed from app/portal/page.tsx — contract unchanged
    - AppCard CTA hrefs (/portal/app/{slug}, /portal/credentials?app={slug}) — SSO launch entry preserved
    - OnboardingWizard gate in page.tsx (needsOnboarding) untouched
  prohibitions:
    - statement: MUST NOT modify lib/portal-access.ts / lib/portal-layout.ts (OPD-1 — getAccessiblePortalApps / getPortalLayout single source)
      status: resolved
      verification: judgment — `git diff -- lib/portal-access.ts lib/portal-layout.ts` empty; getAccessiblePortalApps absent from touched-file list
    - statement: MUST NOT change the fetch/filter/grouping pipeline in app/portal/page.tsx (no new queries, no client re-sort)
      status: resolved
      verification: judgment — diff on page.tsx is presentation-only (header + empty state block)
    - statement: MUST NOT introduce legacy icon imports, inline style objects, raw hex, or half-step spacing utilities in touched files
      status: resolved
      verification: static scoped grep per D-08
  backstops:
    - statement: Long app names truncate on one line (truncate) and descriptions clamp at two lines (line-clamp-2); no byte/grapheme logic
      verification: backstop — held-out visual test at 375px viewport with a long-name app when an env can render; otherwise documented limitation
  assumptions:
    - "Chip grouping derives from `groups` (getPortalLayout group names), NOT from PortalApp.category — matches GroupedAppGrid's current group model; category strings on AppCard keep their existing display role."
---

<objective>
Portal shell and app grid — token-native, restructured per D-01.

Purpose: Delivers the "channel rack" of UIUX-05: the portal chrome (shell + PortalHeader) and the launch-card grid with category chips, responsive 2/3/4 columns, hover raise, keyboard navigation, and the locked empty-state copy. Access semantics stay frozen (OPD-1); only presentation structure changes (D-01/D-03).

Output: token-native `app/portal/layout.tsx`, `PortalHeader`, `app/portal/page.tsx` (header + empty states), restructured `GroupedAppGrid` (chip filter + responsive grid), and kit-based `AppCard` launch cards.

Frozen behavior: auth guard redirect, `getPortalLayout`/`getAccessiblePortalApps` pipeline, credential-count groupBy, wizard gate, launch hrefs, server-side ordering. Gates per D-08; screenshots bonus (PRE-1).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-CONTEXT.md
@.planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-SPEC.md
@.planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-UI-SPEC.md
@app/portal/layout.tsx
@app/portal/page.tsx
@components/portal/PortalHeader.tsx
@components/portal/GroupedAppGrid.tsx
@components/portal/AppCard.tsx
@components/ui/Card.tsx
@components/ui/Button.tsx
@components/ui/Input.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Shell portal token-native + PortalHeader (topbar sticky, nav accent, menu mobile)</name>
  <files>app/portal/layout.tsx, components/portal/PortalHeader.tsx</files>
  <read_first>
    - app/portal/layout.tsx (file yang diubah — inline #0a0a0a saat ini)
    - components/portal/PortalHeader.tsx (file yang diubah — chrome + icon line-style saat ini)
    - components/ui/Button.tsx (kit — tombol Keluar secondary sm)
    - .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-UI-SPEC.md §2 (kontrak header)
  </read_first>
  <acceptance_criteria>
    - layout.tsx: wrapper div `min-h-screen bg-surface-0 text-text-1` menggantikan background/color inline; getServerSession(portalAuthOptions) guard + redirect("/portal-login") dan NextAuthProvider basePath="/api/portal-auth" TIDAK diubah; <main>{children}</main> dipertahankan.
    - PortalHeader: header `sticky top-0 z-40 border-b border-border bg-surface-1`; inner `flex h-14 items-center justify-between px-4 sm:px-6`; logo wordmark `font-display font-semibold text-text-1` dengan "PORTAL" ber-warna `text-accent` (struktur "PORTAL SSO" dipertahankan); nav desktop `hidden sm:flex` dengan item { Aplikasi: SquaresFour, Kredensial: Key, Pengaturan: GearSix } (Phosphor 16) class `inline-flex items-center gap-2 rounded-control px-4 py-2 text-sm font-semibold`, aktif `bg-accent-subtle text-accent` + `aria-current="page"`, non-aktif `text-text-2 hover:bg-surface-2 hover:text-text-1`; nama user `text-sm text-text-2` (hidden di mobile); Keluar = kit Button variant secondary size sm iconLeft SignOut 16, onClick signOut({ callbackUrl: "/portal-login" }) TIDAK diubah; toggle mobile List/X (Phosphor 16) class `sm:hidden` + panel slide-down `absolute inset-x-0 top-14 border-b border-border bg-surface-1 p-2` dengan row `min-h-11 rounded-control` (icon 16 + label, tutup panel saat klik).
    - Static gates kedua file: `! grep -nE '#[0-9a-fA-F]{3,8}' <files>`; `! grep -n 'style=\{\{' <files>`; `! grep -n 'react-icons' <files>`; ikon icon-only memakai aria-label; hanya size type 12/14/20/24 dan weight 400/600 di file yang disentuh; tanpa utility setengah-grid.
  </acceptance_criteria>
  <action>
    Edit app/portal/layout.tsx hanya pada wrapper visual: ganti nilai warna inline (background gelap + color putih) dengan class token `min-h-screen bg-surface-0 text-text-1`. Semua logika (session guard, redirect, provider, PortalHeader render, main) dibiarkan persis.

    Tulis ulang chrome components/portal/PortalHeader.tsx sesuai kontrak UI-SPEC §2: pertahankan "use client", usePathname, state mobileOpen, array navItems (href/icon/label sama), fungsi isActive, dan handler signOut dengan callbackUrl yang sama. Ganti seluruh style object dan icon line-style dengan class token + Phosphor (import named top-level: SquaresFour, Key, GearSix, SignOut, List, X). Aktifkan `aria-current="page"` pada item aktif (selain visual accent). Tombol toggle mobile dan tombol Keluar diberi aria-label. Panel mobile: ganti class display legacy (desktop-nav/mobile-toggle) dengan `hidden sm:flex` / `sm:hidden`. Jangan menambah item nav atau mengubah href. Pesan commit dalam Bahasa Indonesia.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit (exit 0)
      npx eslint app/portal/layout.tsx components/portal/PortalHeader.tsx (0 errors)
      ! grep -nE '#[0-9a-fA-F]{3,8}' app/portal/layout.tsx components/portal/PortalHeader.tsx
      ! grep -n 'style=\{\{' app/portal/layout.tsx components/portal/PortalHeader.tsx
      ! grep -n 'react-icons' app/portal/layout.tsx components/portal/PortalHeader.tsx
      grep -n 'redirect("/portal-login")' app/portal/layout.tsx (guard dipertahankan)
      grep -n 'signOut' components/portal/PortalHeader.tsx (handler dipertahankan)
    </automated>
  </verify>
  <done>Shell portal dan PortalHeader token-native; guard redirect dan signOut callbackUrl identik; gate tsc/eslint/grep hijau; komit Bahasa Indonesia.</done>
  <reversibility rating="reversible">Presentasi-only; header dapat dikembalikan dari git tanpa dampak perilaku.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restrukturisasi grid — chip kategori + grid responsif + empty states (D-01/D-02)</name>
  <files>app/portal/page.tsx, components/portal/GroupedAppGrid.tsx</files>
  <read_first>
    - app/portal/page.tsx (file yang diubah — pipeline data TIDAK boleh berubah)
    - components/portal/GroupedAppGrid.tsx (file yang diubah — restrukturisasi presentasi)
    - .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-UI-SPEC.md §3 (kontrak grid + chip)
    - .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-CONTEXT.md D-01/D-02/D-03 (keputusan terkunci)
  </read_first>
  <acceptance_criteria>
    - page.tsx: header block token-native — eyebrow "PORTAL SSO" (`text-xs font-semibold tracking-[0.2em] text-accent`), H1 "Aplikasi Saya" `font-display text-2xl font-semibold text-text-1`; pipeline (getServerSession → getPortalLayout → wizard gate → getAccessiblePortalApps → groupBy credential → gridGroups) 100% identik; empty state (gridGroups.length === 0) pindah ke pola empty-card bersama: icon tile 24px SquaresFour + heading "Belum ada aplikasi" + body "Tidak ada aplikasi yang dapat ditampilkan saat ini. Atur visibilitas lewat Pengaturan." + tombol "Buka Pengaturan" (kit Button primary, next/link ke /portal/settings).
    - GroupedAppGrid: props `GridGroup[]` dan interface GridApp/GridGroup TIDAK berubah (D-03); di dalamnya: chip row — "Semua" + satu chip per `g.name`; chip `rounded-full border px-4 py-2 text-xs font-semibold transition-colors duration-150`, non-aktif `border-border bg-surface-1 text-text-2 hover:bg-surface-2 hover:text-text-1`, aktif `border-accent/40 bg-accent-subtle text-accent`; `aria-pressed` di tiap chip; seleksi client-side state (tanpa fetch, tanpa perubahan URL); grid `grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4`; urutan app dalam grup = urutan server (tanpa re-sort); group-scoped empty card "Belum ada aplikasi di grup ini." saat hasil filter kosong (pola yang sama dengan empty utama); tampilkan semua grup saat chip "Semua" aktif.
  </acceptance_criteria>
  <action>
    Edit app/portal/page.tsx: ganti blok header (eyebrow + H1) dan blok empty state dengan class token per acceptance criteria; pindahkan konten empty ke pola icon-tile card (icon Phosphor SquaresFour size 24 dalam tile tinted 56px `bg-surface-2` atau tile accent-subtle, heading font-display text-xl, body text-sm text-text-2, aksi kit Button). Jangan sentuh satu baris pun dari pipeline data: session, needsOnboarding gate, pemanggilan helper akses, groupBy credential, konstruksi gridGroups, maupun pemanggilan OnboardingWizard/GroupedAppGrid. Hapus import icon line-style yang tidak terpakai setelah restyle.

    Tulis ulang components/portal/GroupedAppGrid.tsx: pertahankan ekspor interface GridApp/GridGroup persis, tambahkan state chip aktif (default "Semua") di sisi client (tandai "use client" bila perlu — komponen kini memegang state filter). Render chip row + grid responsif per acceptance criteria; saring grup sesuai chip aktif; render empty-card group-scoped bila hasil saringan kosong. Urutan data tidak diubah. Pesan commit dalam Bahasa Indonesia.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit (exit 0)
      npx eslint app/portal/page.tsx components/portal/GroupedAppGrid.tsx (0 errors)
      ! grep -nE '#[0-9a-fA-F]{3,8}' app/portal/page.tsx components/portal/GroupedAppGrid.tsx
      ! grep -n 'style=\{\{' app/portal/page.tsx components/portal/GroupedAppGrid.tsx
      ! grep -n 'react-icons' app/portal/page.tsx components/portal/GroupedAppGrid.tsx
      grep -c 'getAccessiblePortalApps' app/portal/page.tsx (>= 1 — pipeline dipertahankan)
      grep -c 'groupBy' app/portal/page.tsx (>= 1 — hitungan kredensial dipertahankan)
    </automated>
    <human-check>Opsional (PRE-1): saat env tersedia, cek 375px tanpa overflow horizontal, chip filter bekerja, empty state tampil untuk user tanpa akses.</human-check>
  </verify>
  <done>Grid restrukturisasi dengan chip kategori + grid responsif 1/2/3/4 kolom; empty states memakai copy terkunci ("Belum ada aplikasi" + "Buka Pengaturan"); pipeline data dan kontrak props identik; gate hijau; komit Bahasa Indonesia.</done>
  <reversibility rating="reversible">Presentasi-only; grouping regression dapat dikembalikan komponen-saja (D-01 note).</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: AppCard launch card token-native (visual layer, D-03)</name>
  <files>components/portal/AppCard.tsx</files>
  <read_first>
    - components/portal/AppCard.tsx (file yang diubah)
    - components/ui/Card.tsx (kit — dasar card)
    - .planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-UI-SPEC.md §3 AppCard (kontrak visual)
  </read_first>
  <acceptance_criteria>
    - Props AppCardProps (id, name, slug, description?, logoPath?, category?, credentialCount) TIDAK berubah (D-03).
    - Render: kit Card `p-6` + wrapper `group transition-all duration-150 hover:-translate-y-0.5 hover:shadow-lvl-2 focus-within:outline-2 focus-within:outline-offset-2 focus-within:outline-accent`; header row: logo 40px `rounded-card object-cover` (img dengan alt={name}) ATAU initial tile `bg-surface-3 text-text-2` dengan `name.charAt(0)`; name `text-sm font-semibold text-text-1 truncate`; category `text-xs text-text-3`; description `text-sm text-text-2 line-clamp-2` (tanpa substring manual — clamp CSS menggantikan potongan 100 karakter).
    - Health row `text-xs`: credentialCount > 0 → CheckCircle (Phosphor, warna success) + "{N} akun tersimpan" (digit mono via `font-mono tabular-nums`); 0 → WarningCircle (warna warning) + "Belum ada akun".
    - CTA: credentialCount > 0 → Link primary block `block w-full rounded-control bg-accent py-2 text-center text-sm font-semibold text-white hover:opacity-90` ke `/portal/app/${slug}` dengan `target="_blank" rel="noopener noreferrer"` dan teks "Buka"; selainnya → Link secondary (border border-border bg-surface-1 text-text-1 hover:bg-surface-2) ke `/portal/credentials?app=${slug}` teks "Simpan Kredensial". Satu elemen focusable per card (Link CTA) — urutan keyboard = urutan visual.
    - Static gates: `! grep -nE '#[0-9a-fA-F]{3,8}' <file>`; `! grep -n 'style=\{\{' <file>`; `! grep -n 'react-icons' <file>`; teks ikon/CTA 14px; tanpa utility setengah-grid.
  </acceptance_criteria>
  <action>
    Tulis ulang komponen AppCard pada lapisan visual saja: pertahankan "use client", interface props, dan seluruh href (launch path `/portal/app/{slug}` dengan target _blank rel noopener noreferrer untuk akun >0; path simpan `/portal/credentials?app={slug}`). Ganti seluruh style object dengan kit Card + class token per acceptance criteria; ganti penanda teks health ("✓"/"⚠") dan potongan substring deskripsi manual dengan ikon Phosphor (CheckCircle/WarningCircle, import named top-level) + truncate/line-clamp CSS + digit mono. CTA Buka memakai aksen token (`bg-accent`) — bukan warna merek hardcoded — dengan teks 14px semibold kontras (putih). Jangan ubah logika pemilihan CTA atau href. Pesan commit dalam Bahasa Indonesia.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit (exit 0)
      npx eslint components/portal/AppCard.tsx (0 errors)
      ! grep -nE '#[0-9a-fA-F]{3,8}' components/portal/AppCard.tsx
      ! grep -n 'style=\{\{' components/portal/AppCard.tsx
      ! grep -n 'react-icons' components/portal/AppCard.tsx
      grep -n 'target="_blank"' components/portal/AppCard.tsx (launch baru-tab dipertahankan)
    </automated>
  </verify>
  <done>AppCard token-native berbasis kit dengan health row mono, CTA Buka/Simpan Kredensial sesuai kontrak, launch target _blank utuh; gate hijau; komit Bahasa Indonesia.</done>
  <reversibility rating="reversible">Visual-only; kartu lama dapat dikembalikan dari git.</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces (plan 11-02)

| Symbol / File | Kind | Used By |
|---------------|------|---------|
| `app/portal/layout.tsx` | RESTYLED (shell tokens; guard untouched) | all /portal routes |
| `components/portal/PortalHeader.tsx` | RESTYLED (token topbar + mobile menu) | /portal layout |
| `app/portal/page.tsx` | RESTYLED (header + empty states; pipeline untouched) | /portal |
| `components/portal/GroupedAppGrid.tsx` | RESTRUCTURED (chip filter + responsive grid; props contract intact) | /portal page |
| `components/portal/AppCard.tsx` | RESTYLED (kit launch card; hrefs intact) | GroupedAppGrid |

No new components, no new routes, no API/lib changes. `lib/portal-access.ts` and `lib/portal-layout.ts` untouched (OPD-1).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server render (/portal) → portal DB | Access/visibility data crosses here; pipeline unchanged — only presentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05 | Information disclosure | grid render | low | accept | Visibility semantics unchanged; server-side getAccessiblePortalApps/getPortalLayout (frozen) still decide which apps render — restyle cannot leak apps |
| T-11-06 | Spoofing | category chips | low | accept | Chips are client-side presentation state only; no fetch, no URL change, aria-pressed reflects filter semantics |
| T-11-07 | Tampering | AppCard CTA hrefs | medium | mitigate | Task gates assert target _blank + rel noopener noreferrer + launch/save hrefs unchanged; diff on card is visual-only |
| T-11-08 | XSS | app name/description strings | low | accept | Rendered as React text nodes (auto-escaped); no dangerouslySetInnerHTML introduced; description uses CSS clamp, no HTML injection |
| T-11-SC | Tampering | npm installs | low | accept | No package installs in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` exit 0; scoped eslint 0 errors on the five touched files.
- Static grep on touched files: 0 raw hex, 0 inline style objects, 0 legacy icon imports; Phosphor + tokens only.
- Pipeline-preservation greps: getAccessiblePortalApps / groupBy present in page.tsx; wizard gate intact; guard redirect + signOut callbackUrl lines intact.
- OPD-1: `git diff -- lib/portal-access.ts lib/portal-layout.ts` empty; neither file in the touched-file list.
- Screenshots (both themes, /portal at 375/768/1280px) = bonus (PRE-1); otherwise documented limitation.
</verification>

<success_criteria>
- Portal shell + header token-native; redirect/signOut behavior identical (SPEC R3, R5).
- Grid restructured with category chips + 2/3/4-col responsive grid; empty states use the locked copy ("Belum ada aplikasi" + "Buka Pengaturan" / "Belum ada aplikasi di grup ini.") — SPEC edge R2-empty resolved.
- Launch cards render per UI-SPEC §3 (hover raise, keyboard order, health row, Buka/Simpan Kredensial); SSO launch hrefs untouched.
- Zero API/Prisma/portal-access changes; grep gates green (SPEC R5).
</success_criteria>

<output>
Create `.planning/phases/11-portal-auth-surfaces-current-ready-to-plan/11-02-SUMMARY.md` when done
</output>
